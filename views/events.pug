extends layout

block content
  .container
    .row 
      .col-xs-12
        each venue, i in events
          p #{venue.name}
          each eventarr, timestamp in venue.events 
            if venue.events[timestamp].length > 0
              each event, j in eventarr
                p #{event.activity_Time}
