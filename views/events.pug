extends layout

block content
  .container
    .row 
      .col-xs-12
        each val in events
          if val 
            p= val.activity_startDate
            p= val.venue_Name
            p= val.activity_Time
            for orig_artist in val.orig_artists 
              p= orig_artist.name
        else
          li There are no values
